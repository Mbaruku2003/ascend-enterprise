/**
 * ============================================================================
 * Ascend Enterprise UI
 * Overlay
 * ----------------------------------------------------------------------------
 * Select Constants
 * ============================================================================
 */

import type {
    FloatingPlacement,
} from "../Floating/FloatingPosition.types";

/* -------------------------------------------------------------------------- */
/* Defaults                                                                   */
/* -------------------------------------------------------------------------- */

export const DEFAULT_SELECT_OPEN = false;

export const DEFAULT_SELECT_PORTAL = true;

export const DEFAULT_SELECT_PLACEMENT: FloatingPlacement =
    "bottom-start";

export const DEFAULT_SELECT_OFFSET = 6;

export const DEFAULT_SELECT_CLOSE_ON_SELECT = true;

export const DEFAULT_SELECT_LOOP = true;

export const DEFAULT_SELECT_DISABLED = false;
