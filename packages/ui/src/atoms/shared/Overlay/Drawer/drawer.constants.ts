/**
 * ============================================================================
 * Ascend UI
 * Drawer Constants
 * ============================================================================
 */

import type {
    DrawerPlacement,
    DrawerSize,
} from "./Drawer.types";

export const DEFAULT_DRAWER_PLACEMENT: DrawerPlacement =
    "right";

export const DEFAULT_DRAWER_SIZE: DrawerSize =
    "md";

export const DEFAULT_DRAWER_PORTAL =
    true;

export const DEFAULT_DRAWER_OPEN =
    false;
