/**
 * ============================================================================
 * Ascend Enterprise UI
 * HoverCard
 * ----------------------------------------------------------------------------
 * Default configuration.
 * ============================================================================
 */

import type {
    HoverCardPlacement,
} from "./HoverCard.types";

export const DEFAULT_HOVERCARD_OPEN = false;

export const DEFAULT_HOVERCARD_OPEN_DELAY = 700;

export const DEFAULT_HOVERCARD_CLOSE_DELAY = 300;

export const DEFAULT_HOVERCARD_PLACEMENT: HoverCardPlacement =
    "bottom";

export const DEFAULT_HOVERCARD_OFFSET = 8;

export const DEFAULT_HOVERCARD_COLLISION_PADDING = 8;

export const DEFAULT_HOVERCARD_PORTAL = true;

export const DEFAULT_HOVERCARD_ARROW_SIZE = 8;
