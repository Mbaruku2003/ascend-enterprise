/**
 * ============================================================================
 * Ascend UI
 * Popover Constants
 * ============================================================================
 */

import type {
    PopoverPlacement,
} from "./Popover.types";

export const DEFAULT_POPOVER_OPEN =
    false;

export const DEFAULT_POPOVER_PORTAL =
    true;

export const DEFAULT_POPOVER_OFFSET =
    8;

export const DEFAULT_POPOVER_PLACEMENT: PopoverPlacement =
    "bottom";
