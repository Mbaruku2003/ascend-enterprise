/**
 * ============================================================================
 * Ascend UI
 * Button
 * ============================================================================
 *
 * Public exports for the Button component.
 *
 * Consumers should import Button from this barrel instead of implementation
 * files directly.
 *
 * Example:
 *
 * import { Button } from "@ascend/ui";
 *
 * ============================================================================
 */

export { Button } from "./Button";
export { default } from "./Button";

export type {
  ButtonProps,
  ButtonOwnProps,
  ButtonVariant,
  ButtonSize,
  ButtonShape,
  ButtonType,
  ButtonLoadingPosition,
  ButtonOwnerState,
} from "./Button.types";
